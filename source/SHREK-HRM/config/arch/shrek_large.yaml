# SHREK-Large — ~27M parameters, competes with HRM and AugmentedHRM
# Same parameter count as HRM but with SHREK improvements:
#   - universal flip rate + learned error estimator (no task rules)
#   - error injection replaces random perturbation
#   - stagnation delta fed into Q-head
#   - Q-target cached in carry (no second forward pass)
#
# Same base config as hrm_v1.yaml (expansion=4, halt_max_steps=16)
# with SHREK-specific components in the model code.

name: hrm.hrm_act_v1@HierarchicalReasoningModel_ACTV1
loss:
  name: losses@ACTLossHead
  loss_type: stablemax_cross_entropy

halt_exploration_prob: 0.1
halt_max_steps: 16  # same as HRM — fair comparison at equal compute

H_cycles: 2
L_cycles: 2

H_layers: 4
L_layers: 4

hidden_size: 512
num_heads: 8   # head_dim = 512/8 = 64
expansion: 4   # same as HRM baseline — fair comparison

puzzle_emb_ndim: ${.hidden_size}

pos_encodings: rope

# SHREK ablation flags — override from training scripts to disable components
enable_error_injection: true    # Error-Conditioned Injection (flip rate + learned estimator)
enable_stagnation_delta: true   # Stagnation-Aware Q-head (delta fed to Q-head)
